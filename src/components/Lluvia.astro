<div class="rain-container">
    <div class="heart">❤️</div>
    <div class="heart">❤️</div>
    <div class="heart">❤️</div>
    <div class="heart">❤️</div>
    <div class="heart">❤️</div>
    <div class="heart">❤️</div>
    <div class="heart">❤️</div>
    <div class="heart">❤️</div>
</div>

<style>
    .rain-container {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none; /* Importante: deja hacer clic en el mapa a través de los corazones */
        z-index: 0; /* Detrás de todo */
        overflow: hidden;
    }

    .heart {
        position: absolute;
        top: -10vh;
        font-size: 1.5rem;
        opacity: 0.8;
        animation: fall linear infinite;
    }

    /* Configuramos cada corazón con velocidades y posiciones distintas */
    .heart:nth-child(1) { left: 10%; animation-duration: 8s; animation-delay: 0s; }
    .heart:nth-child(2) { left: 20%; animation-duration: 12s; animation-delay: 2s; font-size: 1rem; }
    .heart:nth-child(3) { left: 35%; animation-duration: 10s; animation-delay: 4s; }
    .heart:nth-child(4) { left: 50%; animation-duration: 15s; animation-delay: 1s; font-size: 2rem;}
    .heart:nth-child(5) { left: 65%; animation-duration: 9s; animation-delay: 3s; }
    .heart:nth-child(6) { left: 80%; animation-duration: 11s; animation-delay: 5s; font-size: 1.2rem;}
    .heart:nth-child(7) { left: 90%; animation-duration: 13s; animation-delay: 2s; }
    .heart:nth-child(8) { left: 5%; animation-duration: 14s; animation-delay: 6s; }

    @keyframes fall {
        0% { transform: translateY(-10vh) rotate(0deg); opacity: 0.8; }
        100% { transform: translateY(110vh) rotate(360deg); opacity: 0; }
    }
</style>